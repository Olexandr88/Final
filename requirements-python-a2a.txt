# Python A2A MCP Client Dependencies

# Core async/websocket
websockets>=12.0
asyncio-mqtt>=0.16.1  # For MQTT integration (optional)

# High-performance event loop
uvloop>=0.19.0  # 2-4x faster than asyncio (Linux/macOS only)

# Alternative async runtimes
trio>=0.24.0  # Async I/O with structured concurrency
twisted>=23.10.0  # Event-driven networking
eventlet>=0.35.0  # Async with WSGI support
gevent>=23.9.1  # Greenlet-based concurrency

# Concurrent/parallel execution (stdlib - no install needed)
# concurrent.futures
# multiprocessing

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-timeout>=2.2.0

# Type checking
mypy>=1.7.0
types-websockets

# Code quality
black>=23.11.0
flake8>=6.1.0
isort>=5.12.0

# Development
ipython>=8.17.0
python-dotenv>=1.0.0

# Optional: Performance monitoring
aiomonitor>=0.6.0  # Real-time monitoring of asyncio apps

# ============================================================================
# FUNCTIONAL PROGRAMMING LIBRARIES (for python-a2a-mcp-client-functional.py)
# ============================================================================

# Type-safe error handling with monads (HIGHEST PRIORITY)
returns>=0.22.0  # Result/Maybe monads, railway-oriented programming

# High-performance functional utilities - Cython implementation (HIGHEST PRIORITY)
cytoolz>=0.12.3  # 2-4x faster than pure Python toolz

# Production-ready decorators and utilities (HIGH PRIORITY)
funcy>=2.0  # retry, timeout, caching, flow control

# Advanced iteration recipes (GOOD FIT)
more-itertools>=10.1.0  # Batching, windowing, streaming operations

# Pure Python functional utilities (fallback if cytoolz unavailable)
toolz>=0.12.0  # Compose, pipe, curry, thread-first/last
